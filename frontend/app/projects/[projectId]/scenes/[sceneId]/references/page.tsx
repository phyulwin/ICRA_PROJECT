// frontend/app/projects/[projectId]/scenes/[sceneId]/references/page.tsx
import { ReferenceWorkspace } from '@/components/reference-workspace';

// Render the active Phase 3 scene reference workflow.
export default function ReferencesPage() {
    return <ReferenceWorkspace />;
}
