// frontend/postcss.config.mjs
// Configure Tailwind CSS through the current PostCSS integration.
const config = {
    plugins: {
        '@tailwindcss/postcss': {},
    },
};

export default config;

